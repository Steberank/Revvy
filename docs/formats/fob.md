# `.fob`

Objetos interactivos, 56 bytes: tipo, tres enteros, un flag, posición y dos direcciones. El rayito de stock es el tipo 30: en `nhood1` es el grupo que cae más cerca de los POS nodes (27 instancias). No hay tabla de odds en el binario; `PickupSpawn.odds` queda en `None`.
