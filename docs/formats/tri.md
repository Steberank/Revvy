# `.tri`

Triggers. Count y registros de 68 bytes: tipo, flag, centro, matriz y tamaño. En `nhood1` el tipo 2 es el reposition que devuelve el auto a la pista; esos pasan a `kill_volumes`. El resto se lee y no crea volumen.
