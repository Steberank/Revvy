# `.fin`

Instancias, no la grilla. `i32` cantidad y registros de 72 bytes: nombre de 9 bytes, color, flag, posición y matriz 3×3. El `.prm` del mismo nombre en la carpeta del nivel se transforma y entra al visual. La grilla sale de `STARTPOS` / `STARTROT` del `.inf`.
