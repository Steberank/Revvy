# TrackLayout / `layout.ron`

El ejemplo canónico está en `crates/formats/schema/layout.ron`.

El tipo Rust (`crates/formats/src/layout.rs`) y la validación del editor llegan en fases posteriores. Hasta entonces este archivo describe la forma: grilla, zonas, POS, AI, pickups, campos de fuerza, superficies, `param_mods` y kill volumes.
